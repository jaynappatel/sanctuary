/** @type {import("next").NextConfig} */
const nextConfig = {
  reactStrictMode: true, // Optional but recommended
  swcMinify: true        // Optional, speeds up builds
  // Remove the empty experimental block unless you’re using specific features
};

module.exports = nextConfig;